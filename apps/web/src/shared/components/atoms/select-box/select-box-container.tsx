import SelectBoxPresenter from './select-box-presenter';
import type { SelectBoxPresenterProps } from './type';

const SelectBoxContainer = (props: SelectBoxPresenterProps) => {
  return <SelectBoxPresenter {...props} />;
};

export default SelectBoxContainer;
