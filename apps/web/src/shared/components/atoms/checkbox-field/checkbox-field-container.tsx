import CheckboxFieldPresenter from './checkbox-field-presenter';
import type { CheckboxFieldPresenterProps } from './type';

const CheckboxFieldContainer = (props: CheckboxFieldPresenterProps) => {
  return <CheckboxFieldPresenter {...props} />;
};

export default CheckboxFieldContainer;
